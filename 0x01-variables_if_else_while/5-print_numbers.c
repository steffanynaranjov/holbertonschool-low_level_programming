#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
#include <stdio.h>
int main(void)
{
int num = 0;

while (num < 10)
printf("%d", num++);

printf("\n");
return (0);
}
